# Agent instructions

Read `README.md` and `docs/CURRICULUM.md` before changing the app. This project is now a terminal-first Node.js CLI. Keep version 1 dependency-free unless a new dependency is clearly justified. Keep lesson data separate from terminal rendering. Prefer small modules, accessible text output, and beginner-friendly wording. Run `npm run validate` before declaring work complete. Do not copy APUE text. Preserve backward compatibility for `~/.modern-unix-c/progress.json` when possible.
